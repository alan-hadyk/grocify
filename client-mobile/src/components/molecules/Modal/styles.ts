import { Sx } from "dripsy"

export const modalDefaultStyles: {
  modalContainer: Sx
  subtitleWithIcon: Sx
} = {
  modalContainer: {
    backgroundColor: "$white",
    display: "flex",
    height: "100%",
  },
  subtitleWithIcon: {
    paddingTop: "$16",
    paddingX: "$16",
  },
}
