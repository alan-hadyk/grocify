import { Sx } from "dripsy"

export const modalDefaultStyles: {
  wrapper: Sx
} = {
  wrapper: {
    backgroundColor: "$white",
    bottom: "$0",
    left: "$0",
    padding: "$16",
    position: "absolute",
    right: "$0",
    top: "$0",
    zIndex: 999,
  },
}
