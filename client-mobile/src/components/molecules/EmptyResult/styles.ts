import { Sx } from "dripsy"

export const emptyResultDefaultStyles: {
  text: Sx
  wrapper: Sx
} = {
  text: {
    color: "$gray400",
  },
  wrapper: {
    alignItems: "center",
    gap: "$12",
    marginBottom: "$28",
    marginTop: "$28",
  },
}
