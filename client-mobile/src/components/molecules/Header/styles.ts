import { Sx } from "dripsy"

export const headerDefaultStyles: Record<"wrapper", Sx> = {
  wrapper: {
    alignItems: "stretch",
    flexDirection: "row",
    flexWrap: "nowrap",
    height: 40,
    justifyContent: "space-between",
    paddingLeft: "$16",
    paddingRight: "$16",
    paddingTop: "$16",
  },
}
