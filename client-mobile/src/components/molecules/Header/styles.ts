import { Sx } from "dripsy"

export const headerDefaultStyles: Record<"wrapper", Sx> = {
  wrapper: {
    alignItems: "stretch",
    flexDirection: "row",
    flexWrap: "nowrap",
    height: 40,
    justifyContent: "space-between",
    marginLeft: "$16",
    marginRight: "$16",
    marginTop: "$16",
  },
}
