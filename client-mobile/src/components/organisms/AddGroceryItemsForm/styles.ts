import { Sx } from "dripsy"

export const addGroceryItemsFormDefaultStyles: {
  outerWrapper: Sx
  wrapper: Sx
} = {
  outerWrapper: {
    flex: 1,
    justifyContent: "space-between",
  },
  wrapper: {
    gap: "$16",
  },
}
